class StreamAudio {
    constructor(display, callback) {
        // still working on it!
    }

    async start(display) {
        // still working on it!
    }

    stop() {
        // still working on it!
    }
}

module.exports = StreamAudio;